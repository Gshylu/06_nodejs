const Name = "gannoji"; 

console.log(Name);

// REPL feature of node is very useful in experimenting with /node,js codes and to debug javascript codes

// read = reads users input ,parses the input into js data-structure,and stores in memory
//Eval=takes and evaluates the data structures
//print=prints the result
//loop= loops the above command until the user presses ctrl-c twice just like cmd prompt 